title: Programs
icon: bolt

options:
  preview: "{{ page.url }}"

columns:
  main:
    width: 2/3
    fields:
      layout: fields/layout-sections
  sidebar:
    width: 1/3
    sticky: true
    sections:
      preview:
        type: live-preview
        label: Preview
