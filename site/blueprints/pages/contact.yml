title: Contact
icon: email

options:
  preview: "{{ page.url }}"

tabs:
  layout:
    label: Layout
    icon: dashboard
    columns:
      main:
        width: 2/3
        fields:
          layout: fields/layout-sections
      sidebar:
        width: 1/3
        sticky: true
        sections:
          preview:
            type: live-preview
            label: Preview

  inquiries:
    label: Inquiries
    icon: email
    sections:
      inquiries:
        type: pages
        label: Form inquiries
        templates:
          - inquiry
        status: unlisted
        create: false
        empty: No inquiries yet
