title: Team

options:
  preview: "{{ page.url }}"

columns:
  main:
    width: 2/3
    fields:
      heading:
        label: Heading
        type: text
        default: Our Team

      description:
        label: Description
        type: textarea
        size: medium

      members:
        label: Team Members
        type: blocks
        fieldsets:
          - team-member

  sidebar:
    width: 1/3
    sticky: true
    sections:
      preview:
        type: live-preview
        label: Preview
